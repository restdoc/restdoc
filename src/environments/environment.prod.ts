import { version } from "../../package.json";

export const environment = {
  version: version,
  baseurl: "/",
  production: true,
  logo: "/mail/en-US/assets/images/kanban.png",
  projectsKey: "restdoc-projects",
};
